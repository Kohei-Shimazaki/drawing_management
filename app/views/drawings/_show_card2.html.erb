<div data-toggle="tooltip" title="図面を作成するために必要なタスクを登録できます">
  <%= link_to t("activerecord.models.task")+t("crud.new"), new_task_path(drawing_id: @drawing.id),
      class: "btn btn-primary btn-block mb-1", id: "new_task_btn" %>
</div>
<%= render partial: "tasks/table", locals: { tasks: @drawing.tasks } %>
