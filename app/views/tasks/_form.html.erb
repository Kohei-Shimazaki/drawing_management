<% if @task.errors.any? %>
  <ul>
    <% @task.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
<%= form_with(model: @task, local: true) do |form| %>
  <div class="field">
    <%= form.label :title, t("activerecord.attributes.task.title") %>
    <%= form.text_field :title %>
  </div>
  <div class="field">
    <%= form.label :status, t("activerecord.attributes.task.status") %>
    <%= form.select :status, Task.statuses.keys.map { |k| [I18n.t("activerecord.attributes.task.enum.#{k}"), k]} %>
  </div>
  <div class="field">
    <%= form.label :deadline, t("activerecord.attributes.task.deadline") %>
    <%= form.datetime_select :deadline, {} %>
  </div>
  <div class="field">
    <%= form.label :content, t("activerecord.attributes.task.content") %>
    <%= form.text_area :content %>
  </div>
  <%= form.submit t("submit") %>
<% end %>
