<%= link_to notification_notification_reads_path(notification), method: :post, class: "list_link" do %>
  <li class="list-group-item">
    <div class="row">
      <div class="col-md-2">
        <%= image_tag notification.subject.user.icon.variant(resize:'50x50').processed, class: "small_icon" %>
      </div>
      <div class="col-md-10">
        <small class="text-muted"><%= l notification.created_at, format: :short %></small><br>
        <%= notification.team.name %>の<%= notification.subject.user.name %>さんが<br>
        チャットを投稿しました！
      </div>
    </div>
  </li>
<% end %>
